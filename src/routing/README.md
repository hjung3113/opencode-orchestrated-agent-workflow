# Routing module location

Reserved for future internal routing composition. Its external interface stays
the human-invoked `/route` seam; it must remain the sole writer of run-level
state. Slice A0's working implementation remains in `../route.js`.
