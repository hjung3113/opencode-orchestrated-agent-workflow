# Intake module location

Reserved for future request-normalization or adapter code. It must produce a
declared manifest and may not infer material product direction. Slice A0's
working manifest validation remains in `../manifest.js` until a scoped move.
