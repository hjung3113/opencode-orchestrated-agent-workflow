{
  "human_request": "Prepare a bounded sequence of documentation tasks.",
  "objective": "Record a bounded sequence while preparing only its first task.",
  "scope": ["docs/contracts/**", "docs/workflows/**"],
  "allowed_paths": ["docs/contracts/**", "docs/workflows/**"],
  "forbidden_paths": ["src/**", "bin/**"],
  "non_goals": ["Launch a worker", "Promote a later task"],
  "exclusions": ["Automatic execution"],
  "safe_assumptions": ["The sequence is explicitly declared by the intake manifest."],
  "tasks": [
    {
      "id": "task-define-contract",
      "objective": "Define the bounded graph artifact contract.",
      "scope": ["docs/contracts/**"],
      "allowed_paths": ["docs/contracts/**"],
      "forbidden_paths": ["src/**"],
      "non_goals": ["Implement routing behavior"],
      "dependencies": []
    },
    {
      "id": "task-update-workflow",
      "objective": "Update the workflow handoff description.",
      "scope": ["docs/workflows/**"],
      "allowed_paths": ["docs/workflows/**"],
      "forbidden_paths": ["src/**"],
      "non_goals": ["Create a worker claim"],
      "dependencies": ["task-define-contract"]
    }
  ],
  "ambiguity": {
    "classification": "executable"
  }
}
